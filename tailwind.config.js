const { defaultPreset } = require('tailwindcss-miniprogram-preset')
module.exports = {
    // content: ["*.{mpx,js}"],
    presets: [defaultPreset],
    theme: {
      extend: {}
    },
    plugins: [],
  }